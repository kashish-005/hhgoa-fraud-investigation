[project]
name = "hhgoa-fraud-investigation"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pandas",
    "python-dotenv",
    "pyTigerGraph",
]

[tool.vercel]
entrypoint = "backend.app.main:app"